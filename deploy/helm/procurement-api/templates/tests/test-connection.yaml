apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "procurement-api.fullname" . }}-test-connection"
  labels:
    {{- include "procurement-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "procurement-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
