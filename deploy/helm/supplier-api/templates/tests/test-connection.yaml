apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "supplier-api.fullname" . }}-test-connection"
  labels:
    {{- include "supplier-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "supplier-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
